import React from 'react';

const Hoc=(props)=>props.children;

export default Hoc;

/**--------------Higher Order Components----------------
 * --> this component is used wrape html elements, with out using unnecessary div.  
 * 
 */